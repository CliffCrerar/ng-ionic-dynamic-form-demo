import { TemplateLoggerPipe } from './template-loggere.pipe';

describe('TemplateLoggerPipe', () => {
  it('create an instance', () => {
    const pipe = new TemplateLoggerPipe();
    expect(pipe).toBeTruthy();
  });
});
